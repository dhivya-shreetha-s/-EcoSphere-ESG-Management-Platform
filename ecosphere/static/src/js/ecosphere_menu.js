/** @odoo-module **/
/**
 * EcoSphere — Phase 1 JS stub.
 *
 * This file is intentionally empty in Phase 1.
 * It exists solely so the manifest's assets declaration resolves without error.
 *
 * Phase 4 will add:
 *   - EsgScoreRing OWL component
 *   - EsgCategoryChart OWL component
 *   - EsgAlertCard OWL component
 *   - EsgAiChatPanel OWL component
 *   - EsgTrendLine OWL component
 *   - Three dashboard root components (Employee / Manager / Admin)
 *
 * IMPORTANT: Never import or use the Grok API key here.
 * All AI calls go through the Python controller layer.
 */

// intentionally empty — Phase 4 fills this in
